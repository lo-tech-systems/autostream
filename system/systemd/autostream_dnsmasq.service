[Unit]
Description=AP-only dnsmasq for captive portal
After=network-online.target
Wants=network-online.target

[Service]
Type=simple
ExecStart=/usr/sbin/dnsmasq --no-daemon --conf-file=/etc/dnsmasq.d/autostream-setup.conf
Restart=on-failure

[Install]
WantedBy=multi-user.target
