# /etc/modules-load.d/autostream-bluetooth.conf
#
# This file is part of autostream.
# Copyright (c) 2026 Lo-tech Systems Limited. All rights reserved.
#
# Installed only when Bluetooth input is enabled (BLUETOOTH_MODE=on).
# Loads the kernel ALSA loopback used as the pseudo-device between the
# Bluetooth audio pump and the existing capture pipeline. See
# installer/lib/bluetooth.sh.
snd-aloop
